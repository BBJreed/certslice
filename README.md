# CertSlice

Split `.pfx` / `.p12` into PEM files in the browser. The private key is not uploaded.

- `/pfx-to-pem`
- `/p12-to-pem`
- `/extract-crt-from-pfx`

Two free extracts per day, then $12/mo or $39 lifetime.
